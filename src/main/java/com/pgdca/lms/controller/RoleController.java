package com.pgdca.lms.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.servlet.ModelAndView;

import lombok.extern.slf4j.Slf4j;

@Slf4j
@Controller
public class RoleController {
	/*
	 * @RequestMapping(value = {"/admin"}, method = RequestMethod.GET) public
	 * ModelAndView adminPage() { log.info(this.getClass().getName());
	 * log.info("Enterted"); ModelAndView model = new ModelAndView();
	 * model.setViewName("adminPage"); return model; }
	 */
	
}
